@model WebApplicationLabo4.Models.JugadorClub

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>JugadorClub</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<input type="hidden" asp-for="idJugador" />
            <input type="hidden" asp-for="idClub" />*@
            <div class="form-group">
                <label asp-for="Jugador" class="control-label"></label>
                <select asp-for="Jugador" class="form-control" asp-items="ViewBag.Jugador"></select>
                <span asp-validation-for="Jugador" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Club" class="control-label"></label>
                <select asp-for="Club" class="form-control" asp-items="ViewBag.Club"></select>
                <span asp-validation-for="idClub" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
